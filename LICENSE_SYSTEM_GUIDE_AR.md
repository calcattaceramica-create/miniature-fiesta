# 🔐 دليل نظام التراخيص الكامل
## DED ERP License Management System

---

## 📋 المحتويات

1. [نظرة عامة](#نظرة-عامة)
2. [كيفية عمل النظام](#كيفية-عمل-النظام)
3. [إنشاء ترخيص جديد](#إنشاء-ترخيص-جديد)
4. [إدارة التراخيص](#إدارة-التراخيص)
5. [نشر النظام على Render](#نشر-النظام-على-render)
6. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🎯 نظرة عامة

نظام التراخيص في DED ERP يتيح لك:

✅ **إنشاء تراخيص** لعملاء مختلفين  
✅ **التحكم في الصلاحيات** (عدد المستخدمين، الفروع)  
✅ **تحديد مدة الترخيص** (تجريبي، شهري، سنوي، دائم)  
✅ **مراقبة الاستخدام** (تسجيل كل عملية تحقق)  
✅ **تعليق/تفعيل** التراخيص عن بُعد  
✅ **الحماية التلقائية** - يتم فحص الترخيص قبل كل طلب  

---

## ⚙️ كيفية عمل النظام

### 1. البنية الأساسية

```
app/
├── models_license.py       # نماذج قاعدة البيانات
├── license_manager.py      # منطق إدارة التراخيص
└── license_middleware.py   # فحص تلقائي قبل كل طلب
```

### 2. آلية العمل

```
1. المستخدم يحاول الدخول
   ↓
2. Middleware يفحص الترخيص
   ↓
3. إذا كان صالح → السماح بالدخول
   إذا كان منتهي → عرض صفحة خطأ
   ↓
4. تسجيل عملية الفحص في قاعدة البيانات
```

### 3. أنواع التراخيص

| النوع | المدة | الاستخدام |
|------|------|----------|
| **Trial** | 30 يوم | للتجربة |
| **Monthly** | 30 يوم | اشتراك شهري |
| **Yearly** | 365 يوم | اشتراك سنوي |
| **Lifetime** | دائم | ترخيص دائم |

---

## 🆕 إنشاء ترخيص جديد

### الطريقة 1: باستخدام السكريبت (موصى به)

```bash
python create_license.py
```

سيطلب منك:
- اسم العميل
- بيانات الاتصال (اختياري)
- اسم المستخدم وكلمة المرور للمدير
- نوع الترخيص
- عدد المستخدمين والفروع

**مثال:**
```
Client Name: شركة ABC
Company: ABC Trading
Email: admin@abc.com
Phone: +966501234567
Admin Username: abc_admin
Admin Password: SecurePass123!
License Type: 3 (Yearly)
Max Users: 10
Max Branches: 3
```

### الطريقة 2: برمجياً

```python
from app import create_app, db
from app.license_manager import LicenseManager

app = create_app()
with app.app_context():
    license = LicenseManager.create_license(
        client_name='شركة XYZ',
        admin_username='xyz_admin',
        admin_password='password123',
        license_type='yearly',
        duration_days=365,
        max_users=20,
        max_branches=5,
        client_email='admin@xyz.com',
        client_phone='+966501234567',
        client_company='XYZ Corporation'
    )
    
    print(f"License Key: {license.license_key}")
```

---

## 🛠️ إدارة التراخيص

### عرض جميع التراخيص

```bash
python create_license.py list
```

### تمديد ترخيص

```python
from app.license_manager import LicenseManager

# تمديد 90 يوم
LicenseManager.extend_license(license_id=1, days=90)
```

### تعليق ترخيص (إيقاف مؤقت)

```python
LicenseManager.suspend_license(
    license_id=1, 
    reason="عدم السداد"
)
```

### إلغاء التعليق

```python
LicenseManager.unsuspend_license(license_id=1)
```

### إلغاء التفعيل نهائياً

```python
LicenseManager.deactivate_license(license_id=1)
```

---

## 🚀 نشر النظام على Render

### الخطوة 1: تحديث قاعدة البيانات

قم برفع الملفات إلى GitHub:

```bash
git add .
git commit -m "Add license system"
git push origin main
```

### الخطوة 2: تشغيل سكريبت إضافة الجداول

في Render Shell:

```bash
python add_license_to_render.py
```

هذا السكريبت سيقوم بـ:
- ✅ إنشاء جداول التراخيص
- ✅ إنشاء ترخيص تجريبي (365 يوم)
- ✅ التحقق من صحة النظام

### الخطوة 3: التحقق من التفعيل

افتح التطبيق على Render وحاول تسجيل الدخول:
- إذا ظهرت صفحة تسجيل الدخول → النظام يعمل ✅
- إذا ظهرت صفحة خطأ ترخيص → راجع الخطوات

---

## 🔍 استكشاف الأخطاء

### المشكلة: "لا يوجد ترخيص مفعّل"

**الحل:**
```bash
python add_license_to_render.py
```

### المشكلة: "الترخيص منتهي الصلاحية"

**الحل:**
```python
# تمديد الترخيص
from app.license_manager import LicenseManager
LicenseManager.extend_license(license_id=1, days=365)
```

### المشكلة: جداول التراخيص غير موجودة

**الحل:**
```python
from app import create_app, db
app = create_app('production')
with app.app_context():
    db.create_all()
```

---

## 📊 مراقبة الاستخدام

### عرض سجل الفحوصات

```python
from app.models_license import LicenseCheck

checks = LicenseCheck.query.order_by(
    LicenseCheck.check_time.desc()
).limit(10).all()

for check in checks:
    print(f"{check.check_time}: {check.check_result}")
```

---

## 🎁 ميزات إضافية

### 1. ربط الترخيص بجهاز معين

```python
license.machine_id = LicenseManager.get_machine_id()
license.ip_address = LicenseManager.get_ip_address()
db.session.commit()
```

### 2. إشعارات انتهاء الصلاحية

النظام يعرض تحذير تلقائي عندما يتبقى 7 أيام أو أقل.

### 3. واجهة إدارة التراخيص

يمكنك الوصول إلى صفحة إدارة التراخيص من:
```
/security/licenses
```

---

## 📞 الدعم

للمساعدة أو الاستفسارات، راجع:
- `LICENSE_QUICK_START.txt` - دليل البدء السريع
- `LICENSE_SYSTEM_SUMMARY.txt` - ملخص النظام

---

**تم إنشاؤه بواسطة DED ERP System** 🚀

