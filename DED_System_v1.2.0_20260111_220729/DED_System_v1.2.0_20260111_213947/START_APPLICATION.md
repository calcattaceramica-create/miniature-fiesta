# 🚀 تشغيل التطبيق

## طريقة سريعة للتشغيل

### Windows

#### الطريقة 1: استخدام setup.bat (موصى به للمرة الأولى)
```batch
# انقر مرتين على الملف
setup.bat
```

هذا السكريبت سيقوم بـ:
- ✅ التحقق من Python
- ✅ إنشاء البيئة الافتراضية
- ✅ تثبيت المتطلبات
- ✅ إنشاء قاعدة البيانات
- ✅ إنشاء البيانات التجريبية (اختياري)

#### الطريقة 2: استخدام start.bat (للتشغيل السريع)
```batch
# انقر مرتين على الملف
start.bat
```

#### الطريقة 3: يدوياً
```batch
# 1. تفعيل البيئة الافتراضية
venv\Scripts\activate

# 2. تشغيل التطبيق
python run.py
```

---

### Linux/Mac

#### الطريقة 1: استخدام start.sh
```bash
# إعطاء صلاحيات التنفيذ
chmod +x start.sh

# تشغيل السكريبت
./start.sh
```

#### الطريقة 2: يدوياً
```bash
# 1. تفعيل البيئة الافتراضية
source venv/bin/activate

# 2. تشغيل التطبيق
python run.py
```

---

## 🌐 الوصول إلى التطبيق

بعد التشغيل، افتح المتصفح على:
```
http://127.0.0.1:5000
```

أو:
```
http://localhost:5000
```

---

## 🔐 بيانات الدخول الافتراضية

### حساب المدير (Admin)
- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`
- **الصلاحيات:** كاملة

### حساب موظف (Employee)
- **اسم المستخدم:** `user1`
- **كلمة المرور:** `user123`
- **الصلاحيات:** محدودة

⚠️ **مهم:** غيّر كلمات المرور الافتراضية فوراً!

---

## 📋 التحقق من التشغيل

### علامات التشغيل الناجح:
```
 * Serving Flask app 'app'
 * Debug mode: on
WARNING: This is a development server.
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

### إذا ظهرت أخطاء:

#### خطأ: "No module named 'flask'"
```bash
# تأكد من تفعيل البيئة الافتراضية
venv\Scripts\activate  # Windows
source venv/bin/activate  # Linux/Mac

# أعد تثبيت المتطلبات
pip install -r requirements.txt
```

#### خطأ: "Address already in use"
```bash
# المنفذ 5000 مستخدم، استخدم منفذ آخر
python run.py --port 5001
```

#### خطأ: "Database not found"
```bash
# أنشئ قاعدة البيانات
python init_db.py
```

---

## 🛑 إيقاف التطبيق

### في Terminal/CMD:
اضغط `Ctrl + C`

### إذا كان يعمل في الخلفية:
```bash
# Windows
taskkill /F /IM python.exe

# Linux/Mac
pkill -f "python run.py"
```

---

## 🔄 إعادة التشغيل

إذا قمت بتعديلات على الكود:

### في وضع التطوير (Debug Mode):
- التطبيق سيعيد التشغيل تلقائياً عند حفظ الملفات

### يدوياً:
1. أوقف التطبيق (`Ctrl + C`)
2. شغّله مرة أخرى (`python run.py`)

---

## 🧪 اختبار التطبيق

### اختبار سريع:
1. ✅ افتح المتصفح على http://127.0.0.1:5000
2. ✅ سجل الدخول بحساب admin
3. ✅ تصفح لوحة التحكم
4. ✅ جرب إضافة موظف جديد
5. ✅ جرب إضافة عميل محتمل في CRM

---

## 📊 الأنظمة المتوفرة

بعد تسجيل الدخول، يمكنك الوصول إلى:

### نظام الموارد البشرية (HR)
- 👥 الموظفين
- ⏰ الحضور والانصراف
- 🏖️ الإجازات
- 💰 الرواتب
- 📝 العقود
- 📊 التقييم
- 🎓 التدريب

### نظام CRM
- 🎯 العملاء المحتملين
- 💼 الفرص
- 📞 التفاعلات
- ✅ المهام
- 📢 الحملات

### أنظمة أخرى
- 📦 المخزون
- 💵 المبيعات
- 🛒 المشتريات
- 💳 المحاسبة
- 🏪 نقاط البيع (POS)
- 📈 التقارير

---

## ⚙️ إعدادات التشغيل

### تغيير المنفذ (Port):
```python
# في ملف run.py
app.run(debug=True, port=8000)  # بدلاً من 5000
```

### تعطيل وضع التطوير:
```python
# في ملف run.py
app.run(debug=False)
```

### السماح بالوصول من الشبكة:
```python
# في ملف run.py
app.run(debug=True, host='0.0.0.0')
```

---

## 📞 الدعم

### إذا واجهت مشاكل:
1. راجع [QUICK_START.md](QUICK_START.md)
2. راجع [FAQ.md](FAQ.md)
3. تحقق من سجلات الأخطاء في Terminal
4. افتح Issue على GitHub

---

## ✅ قائمة التحقق

قبل البدء، تأكد من:
- [ ] Python 3.8+ مثبت
- [ ] البيئة الافتراضية منشأة
- [ ] المتطلبات مثبتة (requirements.txt)
- [ ] قاعدة البيانات منشأة (init_db.py)
- [ ] البيانات التجريبية منشأة (اختياري)
- [ ] المنفذ 5000 متاح

---

**جاهز للانطلاق! 🚀**

افتح المتصفح واستمتع باستخدام النظام!

