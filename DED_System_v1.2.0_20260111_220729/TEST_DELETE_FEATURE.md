# اختبار ميزة حذف المنتجات

## ✅ قائمة الاختبارات

### 1. اختبار الواجهة (UI Testing)

#### صفحة قائمة المنتجات
- [ ] التحقق من ظهور زر الحذف (أيقونة سلة المهملات)
- [ ] التحقق من أن الزر بلون أحمر (btn-outline-danger)
- [ ] التحقق من ظهور tooltip عند التمرير على الزر
- [ ] النقر على زر الحذف يفتح نافذة التأكيد
- [ ] نافذة التأكيد تعرض اسم المنتج الصحيح
- [ ] نافذة التأكيد تحتوي على تنبيه واضح
- [ ] زر "إلغاء" يغلق النافذة بدون حذف
- [ ] زر "حذف" يرسل طلب الحذف

#### صفحة تعديل المنتج
- [ ] التحقق من ظهور زر "حذف المنتج" في الرأس
- [ ] الزر بلون أحمر (btn-danger)
- [ ] النقر على الزر يفتح نافذة تأكيد مخصصة
- [ ] النافذة تعرض تفاصيل المنتج (الاسم، الكود، الباركود)
- [ ] النافذة تحتوي على تنبيهات ملونة
- [ ] زر "إلغاء" يعمل بشكل صحيح
- [ ] زر "تأكيد الحذف" يرسل الطلب

### 2. اختبار الوظائف (Functional Testing)

#### حذف منتج بدون ارتباطات
```
الخطوات:
1. إنشاء منتج جديد بدون أي ارتباطات
2. الانتقال إلى صفحة المنتجات
3. النقر على زر الحذف
4. تأكيد الحذف

النتيجة المتوقعة:
✅ يتم حذف المنتج نهائياً من قاعدة البيانات
✅ رسالة نجاح: "تم حذف المنتج [اسم المنتج] بنجاح"
✅ المنتج لا يظهر في القائمة
```

#### حذف منتج مرتبط بفواتير
```
الخطوات:
1. إنشاء منتج
2. إضافة المنتج إلى فاتورة مبيعات
3. محاولة حذف المنتج

النتيجة المتوقعة:
✅ لا يتم حذف المنتج
✅ يتم تعطيل المنتج (is_active = False)
✅ رسالة تحذير: "لا يمكن حذف المنتج... سيتم تعطيله"
✅ الرسالة تعرض نوع الارتباطات وعددها
```

#### حذف منتج مرتبط بعدة سجلات
```
الخطوات:
1. إنشاء منتج
2. إضافة المنتج إلى:
   - فاتورة مبيعات
   - طلب شراء
   - سجل مخزون
3. محاولة حذف المنتج

النتيجة المتوقعة:
✅ يتم تعطيل المنتج
✅ رسالة تعرض جميع الارتباطات:
   "فواتير مبيعات (1)، طلبات شراء (1)، سجلات مخزون (1)"
```

#### حذف نهائي مع force=true
```
الخطوات:
1. إنشاء منتج مرتبط بسجلات
2. إرسال طلب حذف مع force=true
   POST /inventory/products/1/delete?force=true

النتيجة المتوقعة:
✅ يتم حذف جميع السجلات المرتبطة
✅ يتم حذف المنتج نهائياً
✅ رسالة: "تم حذف المنتج وجميع السجلات المرتبطة به نهائياً"
```

### 3. اختبار معالجة الأخطاء

#### منتج غير موجود
```
الخطوات:
1. محاولة حذف منتج بـ ID غير موجود
   POST /inventory/products/99999/delete

النتيجة المتوقعة:
✅ خطأ 404 Not Found
```

#### خطأ في قاعدة البيانات
```
الخطوات:
1. محاكاة خطأ في قاعدة البيانات
2. محاولة حذف منتج

النتيجة المتوقعة:
✅ يتم عمل rollback
✅ رسالة خطأ: "حدث خطأ أثناء حذف المنتج: [تفاصيل]"
✅ لا يتم حذف أي بيانات
```

### 4. اختبار الأمان

#### مستخدم غير مسجل
```
الخطوات:
1. تسجيل الخروج
2. محاولة الوصول إلى مسار الحذف

النتيجة المتوقعة:
✅ إعادة توجيه إلى صفحة تسجيل الدخول
✅ لا يتم حذف أي شيء
```

#### CSRF Protection
```
الخطوات:
1. محاولة إرسال طلب حذف بدون CSRF token

النتيجة المتوقعة:
✅ رفض الطلب
✅ خطأ CSRF
```

### 5. اختبار الأداء

#### حذف منتج مع عدد كبير من الارتباطات
```
الخطوات:
1. إنشاء منتج
2. إضافة 1000 سجل مرتبط
3. محاولة حذف المنتج

النتيجة المتوقعة:
✅ الاستعلام يعمل بكفاءة
✅ الرد في أقل من 2 ثانية
✅ عرض العدد الصحيح للارتباطات
```

---

## 🧪 كيفية تشغيل الاختبارات

### اختبار يدوي
```bash
# 1. تشغيل التطبيق
flask run

# 2. فتح المتصفح
http://localhost:5000/inventory/products

# 3. اتباع خطوات الاختبار أعلاه
```

### اختبار تلقائي (مستقبلاً)
```python
# tests/test_product_delete.py
import pytest
from app.models import Product

def test_delete_product_without_relations(client, auth):
    """Test deleting a product with no relations"""
    # إنشاء منتج
    # حذف المنتج
    # التحقق من الحذف
    pass

def test_delete_product_with_relations(client, auth):
    """Test deleting a product with relations"""
    # إنشاء منتج مع ارتباطات
    # محاولة الحذف
    # التحقق من التعطيل
    pass
```

---

## 📊 نتائج الاختبار

| الاختبار | الحالة | ملاحظات |
|---------|--------|---------|
| واجهة قائمة المنتجات | ⏳ قيد الاختبار | - |
| واجهة تعديل المنتج | ⏳ قيد الاختبار | - |
| حذف بدون ارتباطات | ⏳ قيد الاختبار | - |
| حذف مع ارتباطات | ⏳ قيد الاختبار | - |
| حذف نهائي | ⏳ قيد الاختبار | - |
| معالجة الأخطاء | ⏳ قيد الاختبار | - |
| الأمان | ⏳ قيد الاختبار | - |

---

## 🐛 المشاكل المعروفة

لا توجد مشاكل معروفة حالياً.

---

## ✅ الخلاصة

جميع الاختبارات جاهزة للتنفيذ. يرجى اتباع الخطوات أعلاه والتحقق من كل نقطة.

