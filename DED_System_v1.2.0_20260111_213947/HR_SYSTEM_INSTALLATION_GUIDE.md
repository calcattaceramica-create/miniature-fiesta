# 📖 دليل التثبيت والتشغيل - نظام إدارة الموارد البشرية

## 📋 المتطلبات

### المتطلبات الأساسية:
- Python 3.8 أو أحدث
- pip (مدير حزم Python)
- متصفح ويب حديث

### المتطلبات الاختيارية:
- Git (للتحديثات)
- Virtual Environment (للعزل)

---

## 🚀 التثبيت

### الخطوة 1: التحقق من Python

```bash
# تحقق من إصدار Python
python --version

# يجب أن يكون 3.8 أو أحدث
```

### الخطوة 2: إنشاء بيئة افتراضية (اختياري ولكن موصى به)

```bash
# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
# في Windows:
venv\Scripts\activate

# في Linux/Mac:
source venv/bin/activate
```

### الخطوة 3: تثبيت المتطلبات

```bash
# تثبيت جميع المكتبات المطلوبة
pip install -r requirements.txt
```

### الخطوة 4: إنشاء قاعدة البيانات

```bash
# إنشاء جداول قاعدة البيانات
python init_db.py
```

### الخطوة 5: إضافة بيانات تجريبية (اختياري)

```bash
# إضافة بيانات تجريبية للاختبار
python seed_hr_data.py
```

---

## ▶️ التشغيل

### الطريقة الأولى: استخدام ملف BAT (Windows)

```bash
# فقط اضغط مرتين على الملف
RUN_HR_SYSTEM.bat
```

### الطريقة الثانية: يدوياً

```bash
# تشغيل التطبيق
python run.py
```

### الطريقة الثالثة: باستخدام Flask

```bash
# تعيين متغيرات البيئة
# في Windows:
set FLASK_APP=run.py
set FLASK_ENV=development

# في Linux/Mac:
export FLASK_APP=run.py
export FLASK_ENV=development

# تشغيل التطبيق
flask run
```

---

## 🌐 الوصول إلى النظام

بعد التشغيل، افتح المتصفح على:

```
http://localhost:5000/hr/dashboard
```

أو

```
http://127.0.0.1:5000/hr/dashboard
```

---

## 🔧 الإعدادات

### تغيير المنفذ:

```bash
# تشغيل على منفذ مختلف
flask run --port 8000
```

### تفعيل وضع التطوير:

```bash
# في Windows:
set FLASK_ENV=development

# في Linux/Mac:
export FLASK_ENV=development
```

### تفعيل Debug Mode:

```bash
# في Windows:
set FLASK_DEBUG=1

# في Linux/Mac:
export FLASK_DEBUG=1
```

---

## 📊 البيانات التجريبية

عند تشغيل `seed_hr_data.py` سيتم إضافة:

### الأقسام (5):
- تقنية المعلومات
- الموارد البشرية
- المحاسبة
- المبيعات
- المشتريات

### المناصب (5):
- مدير
- مطور برمجيات
- محاسب
- مندوب مبيعات
- موظف مشتريات

### أنواع الإجازات (4):
- إجازة سنوية (30 يوم)
- إجازة مرضية (15 يوم)
- إجازة طارئة (5 أيام)
- إجازة بدون راتب

### الموظفين (3):
- محمد أحمد (تقنية المعلومات)
- فاطمة علي (الموارد البشرية)
- أحمد محمود (المحاسبة)

---

## 🐛 حل المشاكل

### المشكلة: خطأ في تثبيت المتطلبات

**الحل:**
```bash
# تحديث pip
python -m pip install --upgrade pip

# إعادة تثبيت المتطلبات
pip install -r requirements.txt --force-reinstall
```

### المشكلة: خطأ في قاعدة البيانات

**الحل:**
```bash
# حذف قاعدة البيانات القديمة
del erp_system.db

# إنشاء قاعدة بيانات جديدة
python init_db.py
```

### المشكلة: المنفذ 5000 مستخدم

**الحل:**
```bash
# استخدم منفذ آخر
flask run --port 8000
```

### المشكلة: خطأ في الاستيراد

**الحل:**
```bash
# تأكد من تفعيل البيئة الافتراضية
venv\Scripts\activate

# تأكد من تثبيت المتطلبات
pip install -r requirements.txt
```

---

## 🔄 التحديث

### تحديث المكتبات:

```bash
# تحديث جميع المكتبات
pip install -r requirements.txt --upgrade
```

### تحديث قاعدة البيانات:

```bash
# إذا كنت تستخدم Flask-Migrate
flask db upgrade
```

---

## 🗑️ إلغاء التثبيت

### حذف البيئة الافتراضية:

```bash
# إلغاء تفعيل البيئة
deactivate

# حذف المجلد
rmdir /s venv
```

### حذف قاعدة البيانات:

```bash
del erp_system.db
```

---

## 📝 ملاحظات مهمة

1. **البيئة الافتراضية**: موصى بها بشدة لتجنب تعارض المكتبات
2. **قاعدة البيانات**: يتم إنشاؤها تلقائياً في المجلد الرئيسي
3. **الملفات المرفوعة**: يتم حفظها في مجلد `uploads`
4. **الأمان**: غير كلمة السر الافتراضية في الإنتاج

---

## 🎯 الخطوات التالية

بعد التثبيت الناجح:

1. ✅ افتح لوحة التحكم
2. ✅ راجع [دليل البدء السريع](HR_QUICK_START.md)
3. ✅ راجع [دليل المستخدم](HR_SYSTEM_README.md)
4. ✅ ابدأ باستخدام النظام

---

## 🆘 الدعم

### للمساعدة:
- راجع [HR_QUICK_START.md](HR_QUICK_START.md)
- راجع [HR_SYSTEM_README.md](HR_SYSTEM_README.md)
- راجع [HR_INDEX.md](HR_INDEX.md)

---

**تم التطوير بواسطة فريق تطوير نظام ERP** ✨

