# دليل نظام العملة الديناميكي

## 📋 **نظرة عامة**

تم إضافة نظام عملة ديناميكي يسمح بتغيير العملة من إعدادات الشركة وتطبيقها تلقائياً على جميع الفواتير والواجهات.

---

## ⚙️ **كيفية تغيير العملة**

### **من واجهة التطبيق:**

1. سجل دخول كمدير
2. اذهب إلى **الإعدادات** → **بيانات الشركة**
3. اختر العملة من القائمة المنسدلة
4. احفظ التغييرات

### **من سطر الأوامر:**

```bash
python setup_company_currency.py
```

---

## 💻 **استخدام العملة في القوالب (Templates)**

### **1. المتغيرات المتاحة تلقائياً:**

في جميع القوالب، المتغيرات التالية متاحة تلقائياً:

```jinja2
{{ currency_code }}      {# مثال: SAR, USD, EUR #}
{{ currency_symbol }}    {# مثال: ر.س, $, € #}
{{ currency_name }}      {# مثال: ريال سعودي, دولار أمريكي #}
```

### **2. استخدام الفلتر `currency`:**

الطريقة الأسهل لعرض المبالغ مع العملة:

```jinja2
{# الطريقة القديمة #}
{{ "%.2f"|format(invoice.total_amount) }} ر.س

{# الطريقة الجديدة - ديناميكية #}
{{ invoice.total_amount|currency }}
```

### **3. أمثلة عملية:**

#### **في جداول الفواتير:**

```jinja2
<td>{{ invoice.total_amount|currency }}</td>
<td>{{ invoice.paid_amount|currency }}</td>
<td>{{ invoice.remaining_amount|currency }}</td>
```

#### **في تفاصيل الفاتورة:**

```jinja2
<tr>
    <th>الإجمالي قبل الضريبة:</th>
    <td>{{ invoice.subtotal|currency }}</td>
</tr>
<tr>
    <th>الضريبة:</th>
    <td>{{ invoice.tax_amount|currency }}</td>
</tr>
<tr>
    <th>الإجمالي النهائي:</th>
    <td>{{ invoice.total_amount|currency }}</td>
</tr>
```

#### **في JavaScript:**

```javascript
// استخدم المتغيرات المتاحة
const currencySymbol = '{{ currency_symbol }}';
const currencyCode = '{{ currency_code }}';

// عرض المبلغ
document.getElementById('total').textContent = total.toFixed(2) + ' ' + currencySymbol;
```

---

## 🌍 **العملات المتاحة**

| الرمز | الاسم | الرمز |
|------|------|------|
| SAR | ريال سعودي | ر.س |
| USD | دولار أمريكي | $ |
| EUR | يورو | € |
| AED | درهم إماراتي | د.إ |
| KWD | دينار كويتي | د.ك |
| BHD | دينار بحريني | د.ب |
| OMR | ريال عماني | ر.ع |
| QAR | ريال قطري | ر.ق |
| EGP | جنيه مصري | ج.م |

---

## 🔧 **إضافة عملة جديدة**

لإضافة عملة جديدة، عدل ملف `config.py`:

```python
CURRENCIES = {
    'SAR': {'name': 'ريال سعودي', 'symbol': 'ر.س'},
    'USD': {'name': 'دولار أمريكي', 'symbol': '$'},
    # أضف عملة جديدة هنا
    'GBP': {'name': 'جنيه إسترليني', 'symbol': '£'},
}
```

ثم عدل ملف `app/templates/settings/company.html` لإضافة الخيار في القائمة المنسدلة.

---

## 📝 **ملاحظات مهمة**

1. **العملة تُخزن في جدول `companies`** - كل شركة لها عملتها الخاصة
2. **التغيير فوري** - عند تغيير العملة، تتحدث جميع الواجهات تلقائياً
3. **الفواتير القديمة** - تعرض بالعملة الحالية (لا تتأثر البيانات المخزنة)
4. **JavaScript** - استخدم المتغيرات المتاحة في القوالب

---

## 🎯 **أمثلة للتحديث**

### **قبل:**
```jinja2
<td>{{ "%.2f"|format(invoice.total_amount) }} ر.س</td>
```

### **بعد:**
```jinja2
<td>{{ invoice.total_amount|currency }}</td>
```

---

## ✅ **الملفات التي تحتاج تحديث**

لتطبيق العملة الديناميكية على جميع الواجهات، يجب تحديث:

1. `app/templates/sales/invoices.html`
2. `app/templates/sales/invoice_details.html`
3. `app/templates/sales/add_invoice.html`
4. `app/templates/purchases/invoices.html`
5. `app/templates/purchases/invoice_details.html`
6. `app/templates/purchases/add_invoice.html`
7. `app/templates/pos/*.html`
8. `app/templates/reports/*.html`

---

## 🚀 **الخطوات التالية**

1. تشغيل `python setup_company_currency.py` لإعداد العملة
2. تحديث القوالب لاستخدام `|currency` بدلاً من `ر.س` الثابتة
3. اختبار التطبيق مع عملات مختلفة

