# 🔐 دليل الأمان الشامل - Comprehensive Security Guide

## نظرة عامة - Overview

هذا الدليل يوفر معلومات شاملة حول الميزات الأمنية المطبقة في نظام DED وأفضل الممارسات الأمنية.

This guide provides comprehensive information about security features implemented in DED system and security best practices.

---

## 📋 جدول المحتويات - Table of Contents

1. [الميزات الأمنية المطبقة](#الميزات-الأمنية-المطبقة)
2. [حماية المصادقة](#حماية-المصادقة)
3. [أمان الجلسات](#أمان-الجلسات)
4. [حماية الشبكة](#حماية-الشبكة)
5. [تسجيل الأنشطة](#تسجيل-الأنشطة)
6. [أفضل الممارسات](#أفضل-الممارسات)
7. [الإعدادات الموصى بها](#الإعدادات-الموصى-بها)
8. [استكشاف الأخطاء](#استكشاف-الأخطاء)

---

## 🛡️ الميزات الأمنية المطبقة

### ✅ تم التطبيق - Implemented

#### 1. حماية ضد هجمات Brute Force
- ✅ قفل الحساب بعد 5 محاولات فاشلة
- ✅ مدة القفل: 30 دقيقة
- ✅ عداد المحاولات الفاشلة
- ✅ تسجيل جميع المحاولات

#### 2. أمان كلمات المرور
- ✅ الحد الأدنى: 8 أحرف
- ✅ يجب أن تحتوي على حرف كبير
- ✅ يجب أن تحتوي على رقم
- ✅ تشفير قوي (PBKDF2-SHA256)
- ✅ إجبار تغيير كلمة المرور عند أول دخول

#### 3. تسجيل الأنشطة الأمنية
- ✅ تسجيل محاولات الدخول (ناجحة وفاشلة)
- ✅ تسجيل تغييرات كلمة المرور
- ✅ تسجيل عناوين IP
- ✅ تسجيل User Agent
- ✅ مستويات الخطورة (info, warning, critical)

#### 4. إدارة الجلسات
- ✅ انتهاء الجلسة بعد ساعتين
- ✅ تسجيل جميع الجلسات النشطة
- ✅ إمكانية إنهاء الجلسات من لوحة التحكم
- ✅ حماية CSRF
- ✅ HttpOnly Cookies

#### 5. حماية الشبكة
- ✅ Rate Limiting (تحديد معدل الطلبات)
- ✅ قائمة بيضاء للـ IP
- ✅ تسجيل عناوين IP المشبوهة

#### 6. لوحة التحكم الأمنية
- ✅ عرض الإحصائيات الأمنية
- ✅ مراقبة الأنشطة المشبوهة
- ✅ إدارة الجلسات النشطة
- ✅ فتح الحسابات المقفلة
- ✅ إدارة قائمة IP البيضاء

---

## 🔑 حماية المصادقة

### كيفية عمل النظام

#### 1. تسجيل الدخول الناجح
```
1. المستخدم يدخل اسم المستخدم وكلمة المرور
2. النظام يتحقق من وجود المستخدم
3. النظام يتحقق من أن الحساب غير مقفل
4. النظام يتحقق من كلمة المرور
5. النظام يتحقق من أن الحساب نشط
6. تسجيل الدخول الناجح
7. إعادة تعيين عداد المحاولات الفاشلة
8. تسجيل الحدث في سجل الأمان
9. إنشاء جلسة جديدة
```

#### 2. تسجيل الدخول الفاشل
```
1. المستخدم يدخل بيانات خاطئة
2. النظام يزيد عداد المحاولات الفاشلة
3. النظام يسجل الحدث في سجل الأمان
4. إذا وصل العداد إلى 5:
   - قفل الحساب لمدة 30 دقيقة
   - تسجيل حدث قفل الحساب
5. عرض رسالة خطأ للمستخدم
```

### الإعدادات

في ملف `config.py`:

```python
MAX_LOGIN_ATTEMPTS = 5          # عدد المحاولات المسموح بها
ACCOUNT_LOCK_DURATION = 30      # مدة القفل بالدقائق
```

---

## 🔒 أمان الجلسات

### الإعدادات الأمنية

```python
# في config.py
PERMANENT_SESSION_LIFETIME = timedelta(hours=2)  # مدة الجلسة
SESSION_COOKIE_SECURE = True                     # HTTPS فقط (في الإنتاج)
SESSION_COOKIE_HTTPONLY = True                   # منع JavaScript من الوصول
SESSION_COOKIE_SAMESITE = 'Lax'                  # حماية CSRF
SESSION_REFRESH_EACH_REQUEST = True              # تحديث الجلسة عند كل طلب
```

### مراقبة الجلسات

يمكن للمسؤول مراقبة جميع الجلسات النشطة من:
```
القائمة > الأمان > الجلسات النشطة
```

### إنهاء الجلسات

يمكن للمسؤول إنهاء أي جلسة نشطة فوراً.

---

## 🌐 حماية الشبكة

### Rate Limiting

#### استخدام في الكود:

```python
from app.utils.security_helper import rate_limit

@bp.route('/api/data')
@rate_limit(max_requests=10, window_seconds=60)  # 10 طلبات في الدقيقة
def get_data():
    return jsonify({'data': 'value'})
```

#### الإعدادات الافتراضية:
- **تسجيل الدخول**: 5 محاولات في 5 دقائق
- **API العامة**: 60 طلب في الدقيقة
- **API الحساسة**: 10 طلبات في الدقيقة

### قائمة IP البيضاء

#### إضافة IP إلى القائمة البيضاء:

```
1. اذهب إلى: القائمة > الأمان > قائمة IP البيضاء
2. اضغط "إضافة IP جديد"
3. أدخل عنوان IP
4. أدخل وصف (اختياري)
5. احفظ
```

#### استخدام في الكود:

```python
from app.utils.security_helper import require_ip_whitelist

@bp.route('/admin/critical-action')
@require_ip_whitelist
def critical_action():
    # هذا المسار يتطلب IP من القائمة البيضاء
    pass
```

---

## 📊 تسجيل الأنشطة

### أنواع الأحداث المسجلة

| النوع | الوصف | الخطورة |
|------|-------|---------|
| `successful_login` | تسجيل دخول ناجح | info |
| `failed_login_wrong_password` | كلمة مرور خاطئة | warning |
| `failed_login_unknown_user` | مستخدم غير موجود | warning |
| `login_attempt_locked_account` | محاولة دخول لحساب مقفل | warning |
| `login_attempt_inactive_account` | محاولة دخول لحساب غير نشط | warning |
| `logout` | تسجيل خروج | info |
| `password_changed` | تغيير كلمة المرور | info |
| `account_locked` | قفل الحساب | critical |
| `account_unlocked` | فتح الحساب | info |
| `rate_limit_exceeded` | تجاوز حد الطلبات | warning |
| `ip_not_whitelisted` | IP غير مصرح به | warning |

### عرض السجلات

```
القائمة > الأمان > سجلات الأمان
```

يمكن تصفية السجلات حسب:
- نوع الحدث
- مستوى الخطورة
- التاريخ
- المستخدم
- عنوان IP

---

## ✨ أفضل الممارسات

### للمسؤولين

#### 1. كلمات المرور
```
✅ استخدم كلمات مرور قوية (12+ حرف)
✅ غيّر كلمة المرور الافتراضية فوراً
✅ لا تشارك كلمات المرور
✅ غيّر كلمات المرور بشكل دوري (كل 90 يوم)
```

#### 2. الحسابات
```
✅ أنشئ حساب منفصل لكل مستخدم
✅ امنح الصلاحيات الضرورية فقط
✅ عطّل الحسابات غير المستخدمة
✅ راجع الحسابات النشطة بشكل دوري
```

#### 3. المراقبة
```
✅ راجع سجلات الأمان يومياً
✅ راقب المحاولات الفاشلة
✅ راقب الجلسات النشطة
✅ راقب عناوين IP المشبوهة
```

#### 4. النسخ الاحتياطي
```
✅ نسخ احتياطي يومي لقاعدة البيانات
✅ احفظ النسخ في مكان آمن
✅ اختبر استعادة النسخ الاحتياطية
```

### للمستخدمين

#### 1. كلمة المرور
```
✅ استخدم كلمة مرور قوية وفريدة
✅ لا تشارك كلمة مرورك مع أحد
✅ غيّر كلمة المرور إذا شككت في اختراقها
✅ لا تستخدم نفس كلمة المرور في مواقع أخرى
```

#### 2. الجلسات
```
✅ سجّل خروج عند الانتهاء
✅ لا تترك الجهاز مفتوحاً
✅ استخدم "تذكرني" فقط على أجهزتك الشخصية
```

#### 3. الأمان العام
```
✅ لا تفتح روابط مشبوهة
✅ تأكد من عنوان الموقع (URL)
✅ أبلغ عن أي نشاط مشبوه
```

---

## ⚙️ الإعدادات الموصى بها

### للإنتاج (Production)

```python
# في config.py - ProductionConfig

# 1. Secret Key قوي
SECRET_KEY = 'استخدم مفتاح عشوائي قوي هنا'  # 32+ حرف عشوائي

# 2. HTTPS
SESSION_COOKIE_SECURE = True

# 3. قاعدة بيانات آمنة
SQLALCHEMY_DATABASE_URI = 'postgresql://user:pass@localhost/dbname'

# 4. تسجيل الأخطاء
LOGGING_LEVEL = 'WARNING'

# 5. Rate Limiting صارم
MAX_LOGIN_ATTEMPTS = 3
ACCOUNT_LOCK_DURATION = 60  # ساعة واحدة
```

### للتطوير (Development)

```python
# في config.py - DevelopmentConfig

DEBUG = True
SQLALCHEMY_ECHO = True
SESSION_COOKIE_SECURE = False  # لأن localhost يستخدم HTTP
```

---

## 🔧 استكشاف الأخطاء

### المشكلة: حساب مقفل

**الحل:**
```
1. انتظر 30 دقيقة
   أو
2. اتصل بالمسؤول لفتح الحساب من:
   القائمة > الأمان > لوحة التحكم > فتح الحساب
```

### المشكلة: نسيت كلمة المرور

**الحل:**
```
1. اتصل بالمسؤول
2. المسؤول يمكنه إعادة تعيين كلمة المرور من:
   القائمة > الإعدادات > المستخدمون > تعديل > إعادة تعيين كلمة المرور
```

### المشكلة: تم تجاوز حد الطلبات (Rate Limit)

**الحل:**
```
1. انتظر دقيقة واحدة
2. حاول مرة أخرى
3. إذا استمرت المشكلة، اتصل بالمسؤول
```

### المشكلة: IP غير مصرح به

**الحل:**
```
1. تأكد من اتصالك بالشبكة الصحيحة
2. اتصل بالمسؤول لإضافة IP الخاص بك إلى القائمة البيضاء
```

---

## 📞 الدعم والمساعدة

### للإبلاغ عن مشكلة أمنية

```
📧 البريد: security@yourcompany.com
📱 الهاتف: 0500000000
⏰ متاح: 24/7
```

### للحصول على المساعدة

```
📧 البريد: support@yourcompany.com
📱 الهاتف: 0500000000
⏰ متاح: 8 صباحاً - 5 مساءً
```

---

## 📚 مراجع إضافية

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Flask Security Best Practices](https://flask.palletsprojects.com/en/2.3.x/security/)
- [NIST Password Guidelines](https://pages.nist.gov/800-63-3/)

---

## ✅ قائمة التحقق الأمنية

### قبل النشر في الإنتاج

- [ ] تغيير SECRET_KEY
- [ ] تفعيل HTTPS
- [ ] تغيير كلمة مرور admin الافتراضية
- [ ] مراجعة الصلاحيات
- [ ] تفعيل النسخ الاحتياطي التلقائي
- [ ] تفعيل تسجيل الأخطاء
- [ ] اختبار استعادة النسخ الاحتياطية
- [ ] مراجعة إعدادات الجدار الناري
- [ ] تفعيل Rate Limiting
- [ ] إعداد مراقبة الأمان

---

<div align="center">

**تم التحديث:** 2026-01-12  
**الإصدار:** 1.0  

© 2026 DED Management System - All Rights Reserved

</div>

