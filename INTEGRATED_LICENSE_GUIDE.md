# 🔐 دليل نظام الترخيص المتكامل - Complete License System Guide

## 📖 نظرة عامة - Overview

نظام ترخيص متكامل يربط لوحة التحكم بتطبيق DED، حيث يتم إنشاء حساب مستخدم تلقائياً عند إضافة ترخيص جديد.

---

## 🚀 كيفية الاستخدام - How to Use

### 1️⃣ **تشغيل لوحة التحكم**
```bash
python DED_Control_Panel.pyw
```

### 2️⃣ **تشغيل التطبيق أولاً**
- اضغط "▶ تشغيل التطبيق" لإنشاء قاعدة البيانات
- انتظر حتى يظهر "🟢 يعمل"

### 3️⃣ **تطبيق Migration على قاعدة البيانات**
- افتح تبويب "إدارة التراخيص"
- اضغط على زر "🔧 تطبيق Migration"
- انتظر رسالة النجاح

### 4️⃣ **إضافة ترخيص جديد**
املأ النموذج التالي:
- **اسم الشركة**: اسم الشركة أو العميل
- **المدة (أيام)**: عدد أيام صلاحية الترخيص (افتراضي: 365)
- **اسم المستخدم**: اسم المستخدم للدخول إلى التطبيق
- **كلمة المرور**: كلمة مرور قوية
- **البريد الإلكتروني**: بريد العميل
- **رقم الهاتف**: رقم هاتف العميل
- **عدد المستخدمين**: الحد الأقصى للمستخدمين (افتراضي: 10)
- **ملاحظات**: أي ملاحظات إضافية

اضغط "✨ إنشاء ترخيص"

### 5️⃣ **ماذا يحدث تلقائياً؟**
عند إنشاء الترخيص:
1. ✅ يتم إنشاء مفتاح ترخيص فريد
2. ✅ يتم حفظ الترخيص في ملف JSON
3. ✅ يتم إنشاء سجل في جدول `licenses` في قاعدة البيانات
4. ✅ يتم إنشاء حساب مستخدم جديد في جدول `users`
5. ✅ يتم ربط المستخدم بالترخيص

---

## 🔑 تسجيل الدخول إلى التطبيق

بعد إنشاء الترخيص، يمكن للعميل تسجيل الدخول:

1. افتح التطبيق: `http://127.0.0.1:5000`
2. استخدم:
   - **اسم المستخدم**: الذي أدخلته في النموذج
   - **كلمة المرور**: التي أدخلتها في النموذج

---

## 🛡️ التحقق من الترخيص

عند تسجيل الدخول، يتم التحقق من:
- ✅ وجود ترخيص مرتبط بالمستخدم
- ✅ حالة الترخيص (active/suspended/expired)
- ✅ تاريخ انتهاء الصلاحية

إذا كان الترخيص:
- **غير موجود**: رسالة "لا يوجد ترخيص مرتبط بحسابك"
- **موقوف**: رسالة "تم إيقاف ترخيصك مؤقتاً"
- **منتهي**: رسالة "انتهت صلاحية ترخيصك"

---

## 🔧 إدارة التراخيص

### تفعيل ترخيص
1. اختر الترخيص من القائمة
2. اضغط "✅ تفعيل"
3. سيتم تحديث حالة الترخيص والمستخدم في قاعدة البيانات

### إيقاف ترخيص مؤقتاً
1. اختر الترخيص من القائمة
2. اضغط "⏸️ إيقاف مؤقت"
3. لن يتمكن المستخدم من تسجيل الدخول

### حذف ترخيص
1. اختر الترخيص من القائمة
2. اضغط "🗑️ حذف"
3. تأكيد الحذف

---

## 📊 هيكل قاعدة البيانات

### جدول `licenses`
```sql
- id: معرف فريد
- license_key: مفتاح الترخيص (64 حرف)
- company_name: اسم الشركة
- machine_id: معرف الجهاز
- expiry_date: تاريخ الانتهاء
- duration_days: مدة الترخيص بالأيام
- status: الحالة (active/suspended/expired)
- max_users: الحد الأقصى للمستخدمين
- contact_email: البريد الإلكتروني
- contact_phone: رقم الهاتف
```

### جدول `users`
```sql
- id: معرف فريد
- username: اسم المستخدم
- password_hash: كلمة المرور المشفرة
- license_id: معرف الترخيص (Foreign Key)
- is_active: حالة التفعيل
```

---

## 🎯 مميزات النظام

✅ **إنشاء تلقائي للمستخدمين**: عند إضافة ترخيص، يتم إنشاء حساب تلقائياً  
✅ **ربط محكم**: كل مستخدم مرتبط بترخيص واحد  
✅ **تحقق عند الدخول**: لا يمكن الدخول بدون ترخيص صالح  
✅ **إدارة مركزية**: كل شيء من لوحة تحكم واحدة  
✅ **مزامنة فورية**: التغييرات تنعكس مباشرة على قاعدة البيانات  
✅ **أمان عالي**: كلمات المرور مشفرة بـ Werkzeug  

---

## 🔍 استكشاف الأخطاء

### المشكلة: "Database not found"
**الحل**: 
1. شغل التطبيق مرة واحدة لإنشاء قاعدة البيانات
2. ثم اضغط "🔧 تطبيق Migration"

### المشكلة: "License not found"
**الحل**: تأكد من إنشاء الترخيص أولاً من لوحة التحكم

### المشكلة: "User already exists"
**الحل**: النظام يتعامل مع هذا تلقائياً ويحدث المستخدم الموجود

---

## 📝 ملاحظات مهمة

⚠️ **Admin Account**: حساب `admin` لا يحتاج إلى ترخيص  
⚠️ **Password Security**: استخدم كلمات مرور قوية  
⚠️ **Backup**: احتفظ بنسخة احتياطية من `licenses.json` و `ded.db`  

---

## 🎉 انتهى!

الآن لديك نظام ترخيص متكامل يعمل بشكل كامل! 🚀

