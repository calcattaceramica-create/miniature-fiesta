# دليل البدء السريع - نظام إدارة المخزون المتكامل

## خطوات التشغيل السريعة

### 1. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 2. تهيئة قاعدة البيانات
```bash
flask init-db
```

### 3. تشغيل النظام
```bash
python run.py
```

### 4. فتح المتصفح
افتح المتصفح على العنوان: `http://localhost:5000`

### 5. تسجيل الدخول
- **اسم المستخدم:** admin
- **كلمة المرور:** admin123

---

## الوحدات الرئيسية

### 📦 المخزون
- **المنتجات:** `/inventory/products`
- **التصنيفات:** `/inventory/categories`
- **المخزون:** `/inventory/stock`

### 💰 المبيعات
- **العملاء:** `/sales/customers`
- **الفواتير:** `/sales/invoices`
- **إضافة فاتورة:** `/sales/invoices/add`

### 🛒 المشتريات
- **الموردين:** `/purchases/suppliers`
- **الفواتير:** `/purchases/invoices`
- **إضافة فاتورة:** `/purchases/invoices/add`

### 💵 نقاط البيع
- **واجهة POS:** `/pos/`
- **الورديات:** `/pos/sessions`

### 📊 المحاسبة
- **دليل الحسابات:** `/accounting/accounts`
- **القيود اليومية:** `/accounting/journal-entries`
- **المدفوعات:** `/accounting/payments`

### 👥 الموارد البشرية
- **الموظفين:** `/hr/employees`
- **الأقسام:** `/hr/departments`
- **الحضور:** `/hr/attendance`
- **الرواتب:** `/hr/payroll`

### 📈 التقارير
- **تقارير المبيعات:** `/reports/sales`
- **تقارير المشتريات:** `/reports/purchases`
- **تقارير المخزون:** `/reports/inventory`
- **الأرباح والخسائر:** `/reports/profit-loss`

### ⚙️ الإعدادات
- **بيانات الشركة:** `/settings/company`
- **الفروع:** `/settings/branches`
- **المستخدمين:** `/settings/users`
- **الملف الشخصي:** `/settings/profile`

---

## البيانات الافتراضية

عند تشغيل `flask init-db` يتم إنشاء:

### المستخدمين
- مستخدم مدير: `admin` / `admin123`

### الشركة
- شركة نموذجية بالبيانات الأساسية

### الفرع
- الفرع الرئيسي (BR001)

### الأدوار
- مدير النظام (admin)
- مدير (manager)
- مستخدم (user)

### الوحدات
- قطعة
- كيلوجرام
- متر
- لتر
- صندوق

### المستودع
- المستودع الرئيسي (WH001)

### دليل الحسابات
- الأصول (1000)
- الخصوم (2000)
- حقوق الملكية (3000)
- الإيرادات (4000)
- المصروفات (5000)

---

## نصائح مهمة

### 🔒 الأمان
1. غيّر كلمة مرور المدير فوراً
2. أنشئ مستخدمين بصلاحيات محددة
3. لا تستخدم `admin` في الإنتاج

### 📝 البيانات
1. ابدأ بإدخال بيانات الشركة
2. أضف الفروع والمستودعات
3. أنشئ التصنيفات قبل المنتجات
4. أضف العملاء والموردين

### 🎯 سير العمل الموصى به
1. **إعداد النظام:**
   - بيانات الشركة
   - الفروع والمستودعات
   - المستخدمين والصلاحيات

2. **البيانات الأساسية:**
   - التصنيفات
   - الوحدات
   - دليل الحسابات

3. **الأطراف:**
   - العملاء
   - الموردين

4. **المنتجات:**
   - إضافة المنتجات
   - تحديد الأسعار
   - ضبط المخزون

5. **العمليات:**
   - فواتير الشراء
   - فواتير البيع
   - نقاط البيع

6. **المتابعة:**
   - التقارير اليومية
   - مراجعة المخزون
   - المدفوعات

---

## استكشاف الأخطاء

### المشكلة: لا يعمل التطبيق
**الحل:**
```bash
# تأكد من تفعيل البيئة الافتراضية
venv\Scripts\activate

# تأكد من تثبيت المتطلبات
pip install -r requirements.txt
```

### المشكلة: خطأ في قاعدة البيانات
**الحل:**
```bash
# احذف قاعدة البيانات وأعد إنشائها
del erp_system.db
flask init-db
```

### المشكلة: لا تظهر الواجهة بشكل صحيح
**الحل:**
- تأكد من اتصالك بالإنترنت (Bootstrap و Font Awesome يُحملان من CDN)
- امسح ذاكرة التخزين المؤقت للمتصفح

---

## الدعم

للمساعدة أو الإبلاغ عن مشاكل، يرجى فتح Issue في المستودع.

---

**استمتع باستخدام النظام! 🚀**

