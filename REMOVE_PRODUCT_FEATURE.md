# 🗑️ ميزة إزالة المنتج من الفاتورة

## 🎯 الهدف
تمكين المستخدم من إزالة منتج من الفاتورة أو عرض السعر بسهولة.

---

## ✅ الميزات المضافة

### 1️⃣ **زر حذف لكل منتج**

تم إضافة زر حذف 🗑️ في نهاية كل صف منتج:

<augment_code_snippet path="app/templates/sales/add_invoice.html" mode="EXCERPT">
````html
<td class="text-center">
    <button type="button" class="btn btn-sm btn-danger" 
            onclick="removeItem(${itemCounter})" 
            title="{{ _('Remove Product') }}" 
            data-bs-toggle="tooltip">
        <i class="fas fa-trash"></i>
    </button>
</td>
````
</augment_code_snippet>

---

### 2️⃣ **دالة removeItem محسّنة**

تم تحسين دالة الحذف لتشمل:

#### **أ. التحقق من وجود الصف:**
```javascript
const row = document.getElementById(`item-${index}`);
if (!row) {
    console.error('Row not found:', index);
    return;
}
```

#### **ب. منع حذف آخر منتج:**
```javascript
const tbody = document.getElementById('itemsBody');
const rowCount = tbody.getElementsByTagName('tr').length;

if (rowCount <= 1) {
    alert('{{ _("Cannot remove the last item. At least one product is required.") }}');
    return;
}
```

#### **ج. تأكيد الحذف:**
```javascript
if (confirm('{{ _("Are you sure you want to remove this product?") }}')) {
    row.remove();
    calculateTotals();
    console.log('Item removed:', index);
}
```

---

### 3️⃣ **إعادة حساب الإجماليات تلقائياً**

عند حذف منتج، يتم:
- ✅ إزالة الصف من الجدول
- ✅ إعادة حساب المجموع الفرعي
- ✅ إعادة حساب الضريبة
- ✅ إعادة حساب الإجمالي النهائي

<augment_code_snippet path="app/templates/sales/add_invoice.html" mode="EXCERPT">
````javascript
function removeItem(index) {
    // ... التحقق والتأكيد
    
    row.remove();
    calculateTotals();  // إعادة حساب الإجماليات
}
````
</augment_code_snippet>

---

### 4️⃣ **Tooltip توضيحي**

عند تمرير الماوس على زر الحذف، يظهر نص توضيحي:
- 🇦🇪 "إزالة المنتج"
- 🇬🇧 "Remove Product"

```javascript
// Initialize Bootstrap tooltips
var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'));
var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    return new bootstrap.Tooltip(tooltipTriggerEl);
});
```

---

## 🎬 كيفية الاستخدام

### **الطريقة 1: من صفحة الفاتورة**

1. **افتح صفحة إضافة فاتورة:**
   ```
   http://127.0.0.1:5000/sales/invoices/add
   ```

2. **أضف عدة منتجات:**
   - اضغط "إضافة منتج" عدة مرات
   - اختر منتجات مختلفة

3. **لحذف منتج:**
   - اضغط زر 🗑️ الأحمر في نهاية الصف
   - ستظهر رسالة تأكيد: "هل أنت متأكد من إزالة هذا المنتج؟"
   - اضغط **"OK"** للتأكيد أو **"Cancel"** للإلغاء

4. **النتيجة:**
   - ✅ يتم حذف الصف
   - ✅ يتم إعادة حساب الإجماليات تلقائياً
   - ✅ تتحدث الأرقام في الأسفل

---

### **الطريقة 2: من صفحة عرض السعر**

نفس الخطوات السابقة، لكن من:
```
http://127.0.0.1:5000/sales/quotations/add
```

---

## 🛡️ الحماية والأمان

### **1. منع حذف آخر منتج:**

إذا حاولت حذف المنتج الوحيد، ستظهر رسالة:
```
"لا يمكن إزالة آخر منتج. يجب وجود منتج واحد على الأقل."
```

**السبب:** الفاتورة يجب أن تحتوي على منتج واحد على الأقل.

---

### **2. تأكيد الحذف:**

قبل الحذف، يظهر تأكيد:
```
"هل أنت متأكد من إزالة هذا المنتج؟"
```

**السبب:** منع الحذف العرضي.

---

### **3. معالجة الأخطاء:**

إذا حدث خطأ (مثل: الصف غير موجود):
```javascript
if (!row) {
    console.error('Row not found:', index);
    return;
}
```

**السبب:** منع أخطاء JavaScript.

---

## 📊 مثال عملي

### **السيناريو:**
لديك فاتورة بها 3 منتجات، وتريد حذف المنتج الثاني.

### **الخطوات:**

1. **الفاتورة الأولية:**
   ```
   المنتج 1: لابتوب - 5000 ريال
   المنتج 2: ماوس - 50 ريال
   المنتج 3: كيبورد - 150 ريال
   ─────────────────────────────
   المجموع: 5200 ريال
   ```

2. **اضغط زر 🗑️ بجانب "ماوس"**

3. **تأكيد الحذف:**
   - رسالة: "هل أنت متأكد؟"
   - اضغط "OK"

4. **النتيجة:**
   ```
   المنتج 1: لابتوب - 5000 ريال
   المنتج 3: كيبورد - 150 ريال
   ─────────────────────────────
   المجموع: 5150 ريال ✅ (تم التحديث تلقائياً)
   ```

---

## 🎨 التصميم

### **زر الحذف:**
- 🔴 **اللون:** أحمر (btn-danger)
- 📏 **الحجم:** صغير (btn-sm)
- 🗑️ **الأيقونة:** سلة المهملات (fa-trash)
- 💬 **Tooltip:** "إزالة المنتج"

### **موقع الزر:**
- في آخر عمود من الجدول
- محاذاة في المنتصف (text-center)

---

## 🔧 الكود الكامل

### **HTML:**
```html
<td class="text-center">
    <button type="button" class="btn btn-sm btn-danger" 
            onclick="removeItem(${itemCounter})" 
            title="{{ _('Remove Product') }}" 
            data-bs-toggle="tooltip">
        <i class="fas fa-trash"></i>
    </button>
</td>
```

### **JavaScript:**
```javascript
function removeItem(index) {
    const row = document.getElementById(`item-${index}`);
    if (!row) {
        console.error('Row not found:', index);
        return;
    }
    
    const tbody = document.getElementById('itemsBody');
    const rowCount = tbody.getElementsByTagName('tr').length;
    
    if (rowCount <= 1) {
        alert('{{ _("Cannot remove the last item.") }}');
        return;
    }
    
    if (confirm('{{ _("Are you sure?") }}')) {
        row.remove();
        calculateTotals();
    }
}
```

---

**تاريخ التنفيذ:** 2026-01-14  
**الحالة:** ✅ **مكتمل وجاهز للاستخدام**

