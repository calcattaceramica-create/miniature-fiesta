# 🚀 دليل تطبيق الويب - Web App Guide

## ✅ تم تحويل التطبيق إلى Streamlit!

الآن يمكنك استخدام لوحة التحكم **في المتصفح** بدلاً من تطبيق سطح المكتب!

---

## 📋 المتطلبات - Requirements

### 1. Python مثبت على جهازك
تأكد من تثبيت Python 3.8 أو أحدث

### 2. تثبيت المكتبات المطلوبة

افتح **Command Prompt** أو **PowerShell** واكتب:

```bash
pip install -r requirements_web.txt
```

أو يمكنك تثبيتها يدوياً:

```bash
pip install streamlit pandas werkzeug
```

---

## 🚀 كيفية التشغيل - How to Run

### الخطوة 1: افتح Command Prompt

اضغط `Win + R` واكتب `cmd` ثم اضغط Enter

### الخطوة 2: انتقل إلى مجلد المشروع

```bash
cd C:\Users\DELL\DED
```

### الخطوة 3: شغل التطبيق

```bash
streamlit run DED_Control_Panel_Web.py
```

### الخطوة 4: سيفتح المتصفح تلقائياً!

سيفتح التطبيق على العنوان:
```
http://localhost:8501
```

---

## 🎯 الميزات - Features

### ✅ جميع الميزات من التطبيق الأصلي:

1. **إدارة التراخيص - License Management**
   - ✅ إضافة ترخيص جديد
   - ✅ عرض جميع التراخيص
   - ✅ نسخ مفتاح الترخيص
   - ✅ عرض تفاصيل الترخيص
   - ✅ تعليق الترخيص
   - ✅ حذف الترخيص

2. **التحكم في التطبيق - App Control**
   - ✅ تشغيل التطبيق
   - ✅ إيقاف التطبيق
   - ✅ فتح في المتصفح
   - ✅ تطبيق Migration

3. **إحصائيات - Statistics**
   - ✅ إجمالي التراخيص
   - ✅ التراخيص النشطة
   - ✅ التراخيص المنتهية

4. **واجهة حديثة - Modern UI**
   - ✅ تصميم عصري وجميل
   - ✅ دعم العربية والإنجليزية
   - ✅ متجاوب مع جميع الشاشات
   - ✅ سهل الاستخدام

---

## 📊 الفرق بين النسختين

| الميزة | تطبيق سطح المكتب | تطبيق الويب |
|--------|------------------|-------------|
| **يعمل في المتصفح** | ❌ لا | ✅ نعم |
| **يحتاج تثبيت** | ❌ لا | ✅ نعم (Streamlit) |
| **سهولة الاستخدام** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **التصميم** | Tkinter | Streamlit (أجمل) |
| **يعمل على الموبايل** | ❌ لا | ✅ نعم |
| **مشاركة مع الآخرين** | ❌ صعب | ✅ سهل |
| **جميع الميزات** | ✅ نعم | ✅ نعم |

---

## 🎨 لقطات الشاشة - Screenshots

### 1. الصفحة الرئيسية
- عنوان جميل مع تدرج أزرق
- تبويبات واضحة

### 2. مدير التراخيص
- نموذج إضافة ترخيص
- جدول عرض التراخيص
- إحصائيات مباشرة

### 3. التحكم في التطبيق
- أزرار تشغيل/إيقاف
- حالة التطبيق
- رابط المتصفح

---

## 🔧 استكشاف الأخطاء - Troubleshooting

### المشكلة: `streamlit: command not found`

**الحل:**
```bash
pip install streamlit
```

### المشكلة: التطبيق لا يفتح في المتصفح

**الحل:**
افتح المتصفح يدوياً واذهب إلى:
```
http://localhost:8501
```

### المشكلة: خطأ في قاعدة البيانات

**الحل:**
1. اذهب إلى تبويب "تشغيل التطبيق"
2. اضغط "تشغيل التطبيق" أولاً
3. ثم اضغط "تطبيق Migration"

---

## 🌐 نشر التطبيق - Deploy Online

يمكنك نشر التطبيق **مجاناً** على Streamlit Cloud:

### الخطوة 1: إنشاء حساب على GitHub
إذا لم يكن لديك حساب

### الخطوة 2: رفع الكود على GitHub
```bash
git init
git add .
git commit -m "DED Control Panel Web"
git push
```

### الخطوة 3: الذهاب إلى Streamlit Cloud
https://streamlit.io/cloud

### الخطوة 4: ربط GitHub ونشر التطبيق
- اختر المستودع
- اختر الملف: `DED_Control_Panel_Web.py`
- اضغط Deploy

**سيكون لديك رابط مثل:**
```
https://your-app.streamlit.app
```

---

## 💡 نصائح - Tips

### 1. استخدام على الشبكة المحلية
يمكنك فتح التطبيق من أي جهاز على نفس الشبكة:

```bash
streamlit run DED_Control_Panel_Web.py --server.address 0.0.0.0
```

ثم افتح من أي جهاز:
```
http://YOUR_IP:8501
```

### 2. تغيير المنفذ
إذا كان المنفذ 8501 مستخدم:

```bash
streamlit run DED_Control_Panel_Web.py --server.port 8502
```

### 3. تشغيل في الخلفية
```bash
streamlit run DED_Control_Panel_Web.py &
```

---

## ✅ الخلاصة - Summary

**الآن لديك نسختان:**

1. **تطبيق سطح المكتب** (Tkinter)
   ```bash
   python DED_Control_Panel.pyw
   ```

2. **تطبيق الويب** (Streamlit) ⭐ **الأفضل**
   ```bash
   streamlit run DED_Control_Panel_Web.py
   ```

---

## 🎉 استمتع!

**الآن يمكنك استخدام لوحة التحكم في المتصفح!**

- ✅ واجهة جميلة وحديثة
- ✅ سهل الاستخدام
- ✅ يعمل على أي جهاز
- ✅ مجاني 100%

**جرب الآن! 🚀**

